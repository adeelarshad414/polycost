apiVersion: v1
kind: Service
metadata:
  name: {{ include "polycost.fullname" . }}
  labels:
    {{- include "polycost.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "polycost.selectorLabels" . | nindent 4 }}
