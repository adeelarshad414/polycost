# Feature Spec: <name>

**Status:** Draft
**Owner:** TBD
**Date:** YYYY-MM-DD

## Problem

What user or engineering problem are we solving? Include the target user, current
pain, and why this belongs in the MVP or roadmap phase.

## Acceptance Criteria

- [ ] Observable behavior or outcome 1.
- [ ] Observable behavior or outcome 2.
- [ ] Failure/edge case behavior is defined.

## API Changes

List added, changed, or removed endpoints, request/response shapes, status codes, and
error envelopes. Write `None` if the feature has no API impact.

## Data Changes

List NWS schema changes, database migrations, seed data, equivalence-map changes, and
backward compatibility requirements. Write `None` if there is no data impact.

## UI States

List all frontend states: default, loading, empty, success, partial success, error,
validation, mobile, desktop, and accessibility states. Write `None` for backend-only
work.

## Test Plan

- Unit tests:
- Integration tests:
- E2E tests:
- Security tests:
- Accessibility tests:

## Migration Impact

Describe migration ordering, rollback, data backfill, and local/test database impact.
Write `None` if no migration is required.

## DevOps Impact

Describe CI, Docker, environment, observability, queue, cache, or operational changes.
Write `None` if there is no DevOps impact.

## Cloud Impact

Describe cloud provider assumptions, required secrets, deployment changes, cost impact,
and any resource provisioning. Write `None` if this is local-only.

## Security Impact

Describe auth, input validation, secrets, logging, rate limiting, CORS, headers,
dependency, data exposure, and abuse-case implications.

## Observability

Describe logs, metrics, traces, audit events, dashboards, and alerting that should exist
for this feature.

## Rollout Notes

Describe feature flags, release order, documentation updates, compatibility notes, and
how to verify after deployment.

## Deviations

Record implementation deviations from this spec with date and reason.
